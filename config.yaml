system:
  deployment: bootc
  arch: aarch64

logging:
  kind: basic
  level: debug

wanted:
  checks:
    - type: rootfs_readonly
required:
  checks:
    - type: service_state
      service: sshd.service
      expected: active
